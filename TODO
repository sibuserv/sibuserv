--- TODO ---
* Add support of spec configs (.sibuserv-ci.yml) which will allow users to
  configure, build and install projects in a user defined way.
* Add examples of qmake-, cmake- and autotools-based projects which could be
  built for GNU/Linux, MS Windows and Android.
* Add support of static code analysis using Clang Static Analyzer (from host
  system or from QtCreator >= 4.x).
* Add special proxy daemon for receiving events from GitLab webhooks.
  For example, for checking of commits from merge requests.
! Add support of launching `build-project` in Firejail sandbox.
? Switch to Clang compiler for Android builds by default.
? Add support of sending email message when build of project is failed.
? Add support of static code analysis using PVS-Studio Analyzer
  (https://www.viva64.com/en/pvs-studio/).
? Add support of packaging into Appimage packages (https://appimage.org/).
? Add support of packaging into Flatpak packages (https://flatpak.org/).
? Add support of packaging into Snap packages (https://snapcraft.io/).
? Add support of build for macOS.
